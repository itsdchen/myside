# HL node — actions timeline

Durable chronological record of major actions and changes affecting
n1 (and adjacent infrastructure). Append rows as new actions happen.
Not a deep-dive doc — for context per action, see the linked study or
the `hl_node_perf_consolidated_*` doc.

All times UTC unless noted.

## Timeline

| date | event | impact / link |
|---|---|---|
| **2026-06-09** | **n1 provisioned**: `r6i.4xlarge` in `ap-northeast-1a`, 500 GB gp3 at the AWS default 125 MB/s, no EIP (auto-assigned public IP). `i-0fade2737acb92dd2`. | Recipe in `n1_provision_runbook.md`. |
| 2026-06-09 → 11 | Initial sync, hl-visor + publisher setup, daily ops cron, retention + disk-alert | n1 reached steady-state production around 06-10. |
| 2026-06-11 | C++ publisher port (`hl_node_publisher_cpp`) replaces Python on the books service | Visor CPU 95% → 20%, RSS 411 MB → 165 MB, wire delay 75 min stale → 240 ms avg. |
| **2026-06-13** | **Buffering fix**: `--disable-output-file-buffering` added to the visor systemd unit | Tail-read latency cut by ~220 ms median. Node feed: "+80 ms slower than HL WS" → "−105 ms faster". |
| 2026-06-13 → 19 | Steady-state production. First observations of recurring daily `HL NODE FEED UNHEALTHY` emails — one per consumer process-day | Triggered the investigation that started 06-20. |
| 2026-06-20 | **Snapshot stall investigation kicked off.** Diagnosed two distinct failure modes: snapshot-write contention and peer-network outliers | See `hl_node_perf_consolidated_20260623.md` for the full diagnosis. |
| 2026-06-20 | **E1**: daily `hl_stall_report.sh` cron on n1 (03:30 UTC) | First multi-day visibility into stall counts + snapshot apply durations. |
| 2026-06-20 | **E3, E10**: reconstructed daily outliers (both peer-network events), 11-day peer reliability analysis | Identified ASXN / Luganodes / Hypurrscan / Infinite Field as reliable HL-published operators. |
| 2026-06-22 | **E11 ruled out `reserved_peer_ips` for outbound peer reliability** — field is an inbound VIP allowlist per HL README, not a fallback list | Confirmed by forced-failover test; n2 config reverted. |
| **2026-06-22 02:44** | **E12: n1 gp3 throughput 250 → 1000 MB/s, IOPS 3000 → 4000.** Online EBS modify, zero downtime | Snapshot apply median **32.43 s → 26.40 s (−19 %)**. Snapshot-correlated fast stalls ≥5 s went from **~19/day → 0/day**. ~$35/mo extra cost. |
| 2026-06-22 14:25 | n2 provisioned as `i4i.4xlarge` (intermittent test box, `i-022594c1ec3a4e70b`) | For A/B experiments without touching prod. Lifecycle in `n2_test_box_lifecycle.md` (deleted; folded into consolidated doc). |
| 2026-06-23 | **E15** rejected: i4i NVMe slightly slower than gp3 1000 MB/s; disk benefit saturated | H11 refuted. |
| 2026-06-23 | **E16**: perf-record on n2 during a snapshot apply | Bottleneck = userspace serialization/copy/page-fault on a single dominant thread (84 % hl-node, 12 % libc memcpy, 2.9 % kernel). |
| 2026-06-23 | **E13**: n2 swapped from `i4i.4xlarge` → `r7i.4xlarge` (DDR5) | Snapshot apply median 27.87 → 24.80 s on n2. ~6 % improvement over gp3-bumped n1. |
| 2026-06-23 | **n2 disk-full incident** — missing `/var/lib/hl_data_retain/` lock dir caused retention cron to silently fail since n2 provisioning. Disk hit 100 % in ~22 h | Manual purge of replica_cmds freed 294 GB. Trap documented in both runbooks. |
| 2026-06-23 | **E17**: THP=always enabled on n2 | Additional snapshot apply reduction (median 24.80 → ~24.0 s on n2). Persistence via `/etc/systemd/system/thp-always.service`. |
| 2026-06-23 | n3 spun up as r7i clone of n2 with snapshot + stream tmpfs (E18/E19) | Best test-stack snapshot apply ~22 s. n3 since retired after experiments completed. |
| 2026-06-23 | Docs consolidated: ~22 dated study files folded into `hl_node_perf_consolidated_20260623.md` plus 5 durable ops/playbook docs | Cleanup; new docs land in the consolidated doc or `ws2_open_items.md`, not in new dated files. |
| **2026-06-24 14:02** | **Fleet-wide peer-network outlier**: upstream peer `35.78.199.36` dropped with `early eof`, cascade through 5 peers, ~35 s total feed silence | Classic recurring pattern. Same root cause as 06-19 (30 s) and 06-22 (46 s, 50 s) outliers. Not fixable from our side. |
| **2026-06-24** | **iptables REJECT for `192.168.0.0/16` outbound on n1** | Failed peer-dial attempts to RFC1918 private IPs now fail in <1 ms instead of 5 s TCP timeout. Shaves ~5 s per private-IP attempt from future failover cascades. |
| 2026-06-24 | `iptables-persistent` installed on n1 | Rule persists across reboot. |
| 2026-06-24 | Daily memory tracking added to `hl_stall_report.sh` | Captures VmRSS / VmHWM / Threads / uptime in every morning's report. First data point: 75 GB RSS at 4 d uptime on PID 2913671 — flagged for trend monitoring. |
| 2026-06-24 | **Weekend restart cron** installed on n1 (`/usr/local/bin/hl_weekend_restart.sh`) | Cron: `0 0 * * 6` (Saturday 00:00 EDT = 04:00 UTC). Threshold-gated at 80 GB RSS. `--force` flag for manual override. Emails summary either way. |
| **2026-06-24** | **EIP allocated for n1**: `13.159.57.188` (allocation `eipalloc-0f4cb9edfd69e322a`, tag `n1-feed-eip`) | Quota raised from 5 → 10 via Service Quotas console. Not yet associated. |
| 2026-06-24 21:30 (planned) | **EIP `13.159.57.188` to be associated with n1** | Will change n1's public IP from `43.207.110.196` → `13.159.57.188`. Expected ~5-30 s peer reconnect (consumers fall back to WS, recover). Then notify validating-node operator for `reserved_peer_ips` whitelisting. |
| **2026-06-25 03:30** | **Memory pressure spike on n1** detected via daily stall report: 80 ≥5 s fast-pipeline stalls on 06-24 (vs typical ~2-8 post-E12), 28 already on 06-25 partial. Almost all `outside_snap_window` and not classic peer-loss cascades — systemic slowdown. | hl-node VmHWM reached 119 GB (97 % of 123 GB system RAM) over 5 days uptime, then jemalloc released back to ~33 GB. Approaching OOM territory. |
| **2026-06-25 11:13** | **Manual hl-node restart on n1** (`--force` on `hl_weekend_restart.sh`). PID 2913671 → 112614. RSS 32 GB → 13 GB. Restart took ~9 min to fully catch up via gossip from last local snapshot (block 1048900000). | Memory peak eliminated. Confirmed local snapshot replay path: hl-visor loads `~/hl/hyperliquid_data/abci_state.rmp` then catches up via peer. |
| 2026-06-25 11:17 | Cron migrated from weekly to **daily 4 AM EDT (08:00 UTC) threshold-gated restart** at 70 GB. Most days will skip; restart fires only when memory crosses threshold. | Cron: `0 4 * * * /usr/local/bin/hl_weekend_restart.sh`. |
| **2026-06-25 11:30** | **Both publishers manually restarted** (`hl-publisher-books.service`, `hl-publisher-fills.service`) | Concern about post-restart book-state corruption. Books publisher has `--bootstrap` which seeds initial book state from HL public WS. |
| 2026-06-25 11:35 | Daily restart cron script updated to **also restart both publishers** after hl-node restart, in proper sequence (visor → 60 s sync wait → books → fills → 15 s settle → verify). | Closes the book-state corruption window every time the cron fires. Total restart wallclock ~80-90 s. |
| **2026-06-25 11:55** | **Confirmed hl-node re-streams catch-up events to streaming files**, corrupting publisher book state on every restart. | Evidence: in `node_raw_book_diffs_streaming/hourly/20260625/11`, block `1048908000` first appears at line 5,603,373 (local_time 11:12:36, pre-restart) and again at line 9,874,826 (local_time 11:16:00, post-restart catch-up) — 4.3 M lines apart. Block was streamed twice. Same for all blocks in the range snapshot+1 → chain_head at restart time. Publisher applies each diff twice → silent book corruption. Mitigation already in place: cron script restarts books publisher with `--bootstrap` after every hl-node restart. **TODO**: raise with HL as part of E7 question batch (this is unexpected behavior given the streaming files are documented as live event source); investigate whether all past hl-node restarts (Jun 13, Jun 20, today's) left publishers with hours of corrupted state. |
| 2026-06-25 12:25 | **Post-restart health verification** of all three services on n1 | All `active`: hl-visor / hl-publisher-books (PID 117238) / hl-publisher-fills (PID 117296). ZMQ bound on 5555 and 5556. Chain at head (sub-second lag). No errors in last 5 min. |
| **2026-06-25 12:25** | **hl-node memory grew 13 GB → 23.5 GB in ~1h 12m post-restart** (~9 GB/hour) | Significantly faster than the ~15 GB/day extrapolation from yesterday's data. Either (a) jemalloc arenas reach working size quickly post-restart and growth will plateau soon, (b) actual growth rate is faster than estimated, or (c) snapshot serialization peaks inflate HWM quickly. The 70 GB daily-cron threshold may trip today or tomorrow rather than in 4 days. Watch tomorrow's 03:30 UTC report for cleaner trend data. |
| **2026-06-25 ~17:00** | **n2 re-provisioned** as `r6i.4xlarge` clone of n1 (`i-0e951960c8ddfca65`, EBS gp3 1000 MB/s / 4000 IOPS, ap-northeast-1a). hl-visor brought up under systemd. | Test bench for the publisher restart-resilience changes before n1 deploy. iptables REJECT for `192.168.0.0/16` applied (same as n1). No EIP. |
| **2026-06-25 ~20:00** | **End-to-end test on n2**: `systemctl restart hl-visor` while new publisher tailing → **5,080,000 replay packets dedup'd across ~5,300 blocks, 0 GAP false-fires, BTC top emits remained healthy through the ~5-min recovery window**. | Validates the L1a backward-replay dedup + lazy ratcheting design. Critical bug found and fixed during the test: empty-block skips (~5-6% of blocks on HL Mainnet — visor omits packets when no coin has book diffs) required a 100-block threshold on the L1b forward-gap check, otherwise the publisher would re-bootstrap several times per minute. |
| **2026-06-25 23:01 EDT** | **New publisher binary + cron script staged on n1.** `~/hl_node_publisher_cpp` swapped to the new (L1a/b/c-aware) build; `/usr/local/bin/hl_weekend_restart.sh` swapped to the new (`wait_for_caught_up`) sequence. Running services still on the OLD inode in memory — new code takes effect at the next `systemctl restart`. Backups preserved as `*.bak.20260626`. | Plan: let the regular daily cron at 4 AM EDT 2026-06-26 exercise the new sequence. Morning email is the verification signal. Rollback path documented in PR / chat. |
| **2026-06-26 04:00 EDT** | **Daily restart cron fired and correctly skipped** — RSS=40 GB below 70 GB threshold, no restart needed. New `wait_for_caught_up` sequence not exercised yet. | First fire of the new script. Threshold gating works as designed. |
| **2026-06-26 06:32 EDT** | **New publisher binary auto-deployed via needrestart**, triggered by `apt-daily-upgrade` installing perl. needrestart detected the on-disk `hl_node_publisher_cpp` inode differed from what the running publishers had memory-mapped (the staged binary from 23:01 yesterday), and issued `systemctl restart hl-publisher-books.service hl-publisher-fills.service`. L1c gate released in 2s; `[ws] (re)subscribed all 258 syms` confirmed the always-subscribed model. First hour rotation: `duplicates_dropped=0 gaps_observed=0 small_gaps=1327` over 27 min (matches n2's ~5% empty-block-skip rate). | Lucky verification of the bootstrap path. Note: any future binary swap will auto-pick-up at the next apt-daily-upgrade run via needrestart. |
| **2026-06-26 07:30 EDT** | **`apt-daily-upgrade.timer` constrained to Sundays at 04:30 EDT** via `/etc/systemd/system/apt-daily-upgrade.timer.d/override.conf`. Was: any day, 6:00-7:00 EDT (60-min random jitter). | Reduces surprise restart frequency from daily to weekly, and sequences after our 04:00 EDT `hl_weekend_restart.sh` cron. Trade-off: security updates wait up to 6 days. `apt-daily.timer` (list refresh only) left alone. |
| **2026-06-26 08:46 EDT** | **`hl_weekend_restart.sh` switched from threshold-gated to daily unconditional restart.** Threshold was 70 GB; in practice memory-pressure stalls began around Day 3 (75-100+ GB range), so the threshold cut it close. Daily unconditional restart at 04:00 EDT (the quietest hour) is the right trade now that the new L2 cron handles catch-up cleanly. Email body still records pre-restart `VmRSS / VmHWM` so we can plot the day-over-day growth curve. | Driven by 06-25 stall report (31 ≥5 s stalls before the 11:13 manual restart; 0 after). Threshold-gated logic deleted; `--force` flag kept for symmetry (functionally equivalent now). `/usr/local/bin/hl_weekend_restart.sh.bak.20260626-1246` is the rollback. |
| **2026-06-26 15:55 EDT** | **Deployed `--replica-cmds-style recent-actions` to n1** via `/etc/systemd/system/hl-visor.service.d/replica-cmds-style.conf` drop-in + `hl_weekend_restart.sh --force`. New visor PID 494869, RSS 12 GB. | n2 isolated-variant study (4 variants × 30 min each) found this flag gives the best per-event latency reduction: baseline mean 8.03 s → V1 7.43 s (−0.60 s); max 9.88 s → 9.24 s. The 2 min 18 s cadence between events is structural (every 2000 blocks) and unchanged across variants — V1 just shrinks each event. tmpfs on `replica_cmds` (V3) gave nearly identical improvement but didn't stack with V1 (they target the same userspace serialization). Dropping `--write-hip3-oracle-updates` (V2) was the weakest. Rollback: `rm /etc/systemd/system/hl-visor.service.d/replica-cmds-style.conf && systemctl daemon-reload && hl_weekend_restart.sh --force`. |
| **2026-06-28 05:46 → 17:59 UTC** | **Phase 1 + Phase 2 of consumer-side freshness improvements validated on gf2 over 12 hours.** Phase 1 lowered pkmultifeed's `fallback_silent_s` and `fallback_lag_s` defaults from 5.0 → 1.5 s. Phase 2 removed the `publishing_from_node` binary switch — both node SUB relay and HL public WS arbitrate per-message via the existing per-(sym, chain_time) gate, first source to claim a chain_time wins. Renamed atomic to `node_healthy` (informational only). | gf2 was the canary: 3M+ books, 216k fills processed, zero errors, zero crashes, one `node_healthy` flip at startup (no subsequent flips). The 12:06-12:11 UTC freshness window captured a real slow-apply event — WS was 2 s ahead of node on average, winning 54-73% of samples per 5-min window — proving Phase 2's per-message handoff works during real stalls. No alert emails fired because the slow-apply windows didn't sustain >5 s of silence (the gate handed off WS before the threshold tripped). Launcher rotation between `pkmultifeed_0` and `pkmultifeed_1` slots also exercised cleanly. See `overmind/studies/hl_feed/consumer_freshness_plan_20260626.md`. |

## Notes on this file

- **Append rows; don't rewrite history.** If something is reversed later, add a new row noting the reversal rather than editing the original.
- Per the README's discipline, don't create new dated top-level docs for one-off actions — log them here.
- For deep context on any row, link from the row to the relevant study doc or PR commit.

## Adjacent durable docs

- `README.md` — system architecture and current operational state
- `ws2_open_items.md` — live open / deferred / done tracker
- `hl_node_perf_consolidated_20260623.md` — performance experiment results
- `n1_navigation_guide.md` — commands and troubleshooting recipes
- `n1_provision_runbook.md` — build-a-new-n1 recipe
- `go_live_playbook.md` — production cutover playbook
