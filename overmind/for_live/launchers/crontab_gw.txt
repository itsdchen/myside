# Crontab entry to run pymultifeed at 18:00 Eastern Time daily

# Running it directly with the venv's python because each ; separated command starts up its own shell, which means the activate won't actually activate.

# NOTE: 
# Market and creds will need to change to Hyperliquid for mainnet. 

# Run the daily wsgateway on s/m/t/w
0 18 * * * echo "START: $(date)"; cd /home/ubuntu/estrader/ && /home/ubuntu/.venvs/v1/bin/python gateway/wsgateway.py --market HyperliquidTest --creds /home/ubuntu/.creds/.HyperliquidTest.creds.json --date TOMORROW --wait_secs 5 --id YOUR_ID >> /home/ubuntu/estrader/logs/gw_logs_$(date +\%Y\%m\%d).log 2>&1
